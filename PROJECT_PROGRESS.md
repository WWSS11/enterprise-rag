---
document_type: project-progress
project: enterprise-rag
version: 0.1.0
current_phase: maintenance
current_task: none
next_task: none
last_updated: 2026-08-16
---

# Enterprise RAG 项目说明与任务进度

本文档用于长期记录项目目标、技术架构、已完成功能、当前开发位置、后续任务顺序和验收标准。开发环境迁移、工作区更换或长期维护时，应先阅读本文档，再结合代码和 README 确认具体实现。

## 1. 项目介绍

Enterprise RAG 是面向企业知识库的检索增强生成系统。系统把文档管理、权限控制、异步入库、混合检索、流式问答、引用证据和自动质量评测整合为一个可审计的闭环。

产品定位：**Evidence-first enterprise RAG**。

核心目标：

1. 用户只能检索自己有权限访问的知识库。
2. 回答必须基于真实文档，并提供可以检查的引用证据。
3. 文档解析、Embedding、索引重建和评测等长任务异步执行。
4. RAG 参数、模型、分块和检索策略的变化必须通过评测对比证明效果。
5. 系统需要具备租户隔离、权限、审计、任务历史和质量门禁能力。
6. 通过自动化测试、真实业务评测、安全检查和压测持续验证系统质量。

主要用户：

- 企业知识工作者：使用知识库进行带引用的问答。
- 知识库管理员和文档编辑者：管理知识库、成员、文档与入库任务。
- 评测工程师：维护标准问题、运行质量评测、比较版本并执行质量门禁。
- 系统管理员：查看服务状态、审计日志和执行高风险运维操作。

## 2. 技术架构

| 模块 | 技术 | 主要职责 |
| --- | --- | --- |
| API | FastAPI | REST API、SSE 流式回答、鉴权依赖、OpenAPI |
| RAG 工作流 | LangChain + LangGraph | 查询改写、检索、重排、上下文扩展和生成 |
| 关系数据库 | PostgreSQL | 用户上下文、权限、文档元数据、任务、会话、评测和审计 |
| ORM 与迁移 | SQLAlchemy + Alembic | 数据访问和 Schema 版本迁移 |
| 向量数据库 | Milvus | Dense/Sparse/Hybrid 检索和索引管理 |
| 缓存与消息 | Redis | 限流、分布式锁、Celery broker/result backend |
| 异步任务 | Celery | 入库、删除、索引重建、飞书同步和批量评测 |
| 定时任务 | Celery Beat | 周期性连接器同步 |
| 身份认证 | OIDC/JWT + Keycloak | Authorization Code + PKCE、JWT 验证、角色和群组映射 |
| 前端 | React + TypeScript + Vite | 企业管理控制台和流式问答界面 |
| 国际化 | i18next | `zh-CN` 和 `en-US` |
| 本地编排 | Docker Compose | PostgreSQL、Redis、Milvus、MinIO、etcd、Keycloak |
| 测试 | pytest、Vitest、Playwright | 后端、组件、浏览器和身份认证流程验证 |

系统主链路：

```text
OIDC 登录
  → 租户与知识库权限过滤
  → 查询改写
  → Dense + Sparse/BM25 混合召回
  → Reranker 重排
  → 父级与相邻章节动态扩展
  → LLM 流式生成
  → 引用解析与证据保存
  → 评测、对比与质量门禁
```

## 3. 目录说明

```text
enterprise-rag/
├─ app/                         FastAPI、业务服务、RAG 和 Celery
│  ├─ api/v1/                  API 路由
│  ├─ core/                    配置、日志和异常处理
│  ├─ db/                      SQLAlchemy 会话和模型
│  ├─ rag/                     LangGraph 与检索工作流
│  ├─ schemas/                 Pydantic 请求/响应模型
│  ├─ security/                OIDC、身份和权限边界
│  ├─ services/                入库、检索、评测、任务等服务
│  └─ workers/                 Celery 应用与任务
├─ frontend/                    React 前端、Vitest 和 Playwright
├─ migrations/versions/         Alembic 迁移
├─ infra/                       Docker Compose、镜像版本和 Keycloak Realm
├─ scripts/                     Windows 开发、迁移和部署脚本
├─ docs/                        架构、认证和评测基线文档
├─ tests/                       后端测试
├─ .env.example                 应用配置示例
├─ pyproject.toml               Python 依赖和工具配置
├─ README.md                    安装、启动、API 和操作说明
└─ PROJECT_PROGRESS.md          项目说明与任务进度
```

## 4. 配置与运行约定

### 配置文件

- 根目录 `.env`：后端应用、模型、OIDC、RAG 参数和连接地址。
- `infra/.env`：PostgreSQL、Redis、MinIO、Keycloak 等中间件配置。
- `frontend/.env`：浏览器可以公开读取的 Vite 配置，禁止保存服务端密钥。
- `*.env.example`：配置模板，只能包含安全示例值或占位符。

任何真实 Token、数据库密码、OIDC Secret 和模型密钥都不能提交到 Git。

### 开发运行方式

- Windows 日常开发：FastAPI、Celery Worker、Celery Beat 和前端运行在本地项目环境。
- Python 使用项目目录下的 `.venv`，不污染系统 Python。
- PostgreSQL、Redis、Milvus、MinIO、etcd 和 Keycloak 使用 Docker。
- 完整容器方式用于集成验证和生产部署，不作为日常代码热更新方式。

### 默认本地入口

- 前端：`http://localhost:3000`
- FastAPI：`http://127.0.0.1:8000`
- Keycloak：`http://127.0.0.1:18080`
- FastAPI 文档：`http://127.0.0.1:8000/docs`

具体端口和启动命令以当前 `README.md`、`.env` 和 `infra/compose.yml` 为准。

## 5. 已完成功能

### T01 — 基础架构与配置 `done`

- FastAPI、PostgreSQL、Redis、Milvus、MinIO、etcd、Celery 和 Celery Beat 已接入。
- SQLAlchemy、Alembic 和 pydantic-settings 已建立。
- 根应用配置与中间件配置已经分层。
- Windows 项目内 `.venv` 开发方式已经建立。
- 健康检查、结构化日志、统一 Problem Details 和 `request_id` 已实现。

### T02 — OIDC、租户与权限 `done`

- 浏览器使用 Authorization Code + PKCE。
- JWT 验证签名、`iss`、`aud`、`exp`、`iat` 和 `sub`。
- Audience 强制包含 `enterprise-rag-api`。
- OIDC 模式拒绝客户端伪造身份 Header。
- 用户、租户、角色和群组身份映射已完成。
- 知识库支持 `reader`、`editor`、`owner` 权限。
- 用户和群组 ACL、管理员权限和审计日志已实现。

### T03 — 文档解析与多粒度分块 `done`

- 文档结构解析和语义边界识别已实现。
- 支持 atomic、retrieval、parent 和章节/摘要上下文层级。
- Chunk 保存父子关系、章节路径和上下文信息。
- 支持相邻章节动态扩展和上下文 Token 预算。
- 分块配置会进入评测运行快照，可长期比较。

### T04 — 混合检索、重排与生成 `done`

- Dense 与 Sparse/BM25 思路的混合召回已实现。
- 支持元数据过滤、租户过滤和知识库权限过滤。
- 支持 Reranker、TopK、阈值和失败降级。
- 支持父级/相邻 Chunk 扩展和多文档上下文多样性。
- 支持流式生成、无资料拒答和聊天限流。
- 引用和实际生成上下文分开保存，避免把“模型看过”误报为“答案引用”。

### T05 — 聊天与引用证据 `done`

- 知识库选择和真实流式问答已实现。
- 回答正文、引用、来源证据和引用诊断已实现。
- 会话和消息服务端持久化已实现。
- 支持会话分页、重命名、归档和恢复。
- 页面刷新后可以恢复历史会话。
- 中英文聊天界面已完成。

### T06 — 知识库与成员管理 `done`

- 知识库创建、列表、详情、修改、归档和恢复已实现。
- 支持租户可见和受限知识库。
- 支持用户与群组成员列表、授权和撤销。
- 已实现创建者和最后一个 Owner 的安全限制。
- 前端根据有效权限显示管理操作，后端执行最终鉴权。

### T07 — 文档与索引生命周期 `done`

- 文档上传和状态列表已实现。
- 本地目录扫描已实现。
- 文档重新入库和异步删除已实现。
- 全量索引重建已实现。
- PostgreSQL 与 Milvus 之间使用可恢复补偿流程，而不是伪分布式事务。
- 入库、删除和重建过程具备并发保护和失败补偿。

### T08 — 任务中心与持久化历史 `done`

- 任务状态保存在服务端，不依赖当前浏览器标签页。
- 支持任务类型、状态和知识库过滤。
- 支持分页和单任务详情。
- 文档、索引和评测操作可以跳转查看真实任务状态。

### T09 — RAG 自动评测与质量门禁 `done`

- 评测数据集和标准用例已实现。
- 用例支持分页、筛选、创建、修改和删除。
- 已参与评测的用例具有不可变保护。
- 评测运行、状态轮询、历史、报告和重新计算已实现。
- 支持 Baseline 与 Candidate 对比。
- 支持 Recall@K、MRR、Rerank Recall、引用指标、关键点覆盖、拒答准确率和延迟指标。
- 质量门禁通过返回 200，回退返回 409，可直接用于阻止发布。
- 模型、检索、分块、提示和阈值配置会保存在运行快照中。

### T10 — 企业前端与国际化 `done`

- 已完成认证应用壳、聊天、知识库、文档、任务、评测和系统页面。
- `zh-CN` 为默认语言，同时支持 `en-US`。
- 已建立 Evidence Desk 设计体系。
- 支持桌面侧栏、移动抽屉和响应式布局。
- 支持 loading、empty、403、404、API error 和身份恢复状态。
- 支持键盘焦点、Skip Link、focus trap 和 reduced motion。
- 浏览器 Token 只存放在 `sessionStorage`，不写入 `localStorage`。

### T11 — 控制台业务闭环 `done`

- 已修复连续创建评测用例时的表单竞态。
- 已补齐文档操作、成员管理和管理员索引重建。
- 已补齐任务和评测运行的服务端历史。
- 已补齐聊天会话历史和生命周期。
- 已补齐知识库、文档和评测用例生命周期。
- 前端不再依赖虚构数据或仅当前标签页保存的业务历史。

## 6. 当前开发阶段

当前阶段：`quality-hardening`。

核心业务功能已经进入稳定阶段。后续工作重点是自动化验收、真实业务质量基线、应用安全和性能稳定性，不再继续堆叠页面或重复调整设计系统。

### T12 — CI 与自动质量门禁 `done`

目标：让每个 Pull Request 自动完成代码质量、确定性测试、迁移兼容性和安全检查，尽早阻止代码回归进入主分支。

需要完成并验证：

1. 后端检查：Ruff、mypy、pytest、pip check 和 Alembic 空库迁移。
2. 前端检查：`npm ci`、typecheck、lint、Vitest 和 build。
3. 确定性 E2E：启动测试所需依赖，执行不依赖外部模型的 Playwright，失败时保存 trace 和 screenshot。
4. 质量门禁逻辑：通过后端、前端和确定性 E2E 验证 Baseline/Candidate 比较、通过结果和回退结果；真实业务运行归入 T13。
5. 安全检查：执行 Python/npm 依赖审计、密钥扫描和 SAST。
6. 数据库兼容检查：验证空库升级、已有库升级和迁移降级失败场景。

验收标准：

- Pull Request 必须经过自动检查。
- 后端、前端和确定性 E2E 全部通过。
- 空库与已有数据库迁移验证通过。
- 高危依赖、密钥泄漏、静态安全问题或质量门禁逻辑回归会阻止合并。
- 失败任务保留足够的日志和测试产物用于定位问题。

当前进度（2026-08-04）：

- 已完成后端 Ruff、mypy、pytest、pip check 自动检查。
- 已完成前端 typecheck、ESLint、Vitest 和 build 自动检查。
- 已完成默认不依赖外部服务的 7 条 Playwright E2E，并保留真实 Keycloak/RAG 用例的显式开关。
- 已完成空库升级、迁移回退和再次升级到 Alembic head 的自动验证。
- 已接入 Python/npm 依赖审计、仓库密钥扫描和 Python/TypeScript CodeQL SAST。
- 真实 Baseline/Candidate 业务质量门禁转入 T13，项目许可证或商业授权策略转入 E10，不阻塞 T12。

## 7. 未完成任务与执行顺序

除非出现阻塞问题，后续严格按照以下顺序执行。

### T13 — 真实全链路与业务质量基线 `done`

当前仓库中的工程评测集主要验证项目架构和源码知识，不代表真实企业业务效果。

开始真实业务数据验证前必须满足以下安全条件：

- 测试环境使用独立账号、数据库、对象存储和密钥，不复用其他环境凭据。
- 测试环境只能通过受保护的隔离网络访问。
- 测试资料已经完成不可逆脱敏，并通过允许使用范围确认。
- 已确认 Chat、Embedding 和 Rerank 供应商不会使用测试数据训练模型，并明确数据保留和删除规则。
- 未经批准的敏感数据不得进入测试环境或发送给外部模型。

必须验证以下真实链路，不得使用 API mock 代替：

```text
OIDC 登录
→ 创建知识库并配置权限
→ 上传文档
→ Celery Worker 解析与 Embedding
→ Milvus 写入和混合检索
→ Rerank
→ 真实模型流式回答
→ 引用与证据断言
→ 文档重新入库
→ 文档删除
→ 全量索引重建
```

真实模型链路可按每日计划或人工批准触发，不要求每个 Pull Request 都承担外部模型费用和波动。

Baseline/Candidate 质量门禁在本任务中使用真实业务评测运行验证：先冻结一个已验收运行作为 Baseline，再将后续运行作为 Candidate 比较。当前阶段采用人工触发，不要求接入 Pull Request CI，也不要求新增部署环境。

需要准备：

- 10～50 份脱敏真实业务文档。
- 20～50 条标准问题。
- 每题标准答案、权威文档、可接受引用文档和必答关键点。
- 5～10 条知识库无法回答的拒答题。
- 可用的 Chat、Embedding 和 Rerank 模型额度。
- 首批企业环境预计的知识库、文档、成员、会话和评测数据规模。

需要冻结：

- Retrieval Recall@K 和 MRR。
- Rerank Recall。
- Citation Precision/Recall 和引用证据支撑率。
- 关键点覆盖率。
- 无答案拒答准确率。
- 首 Token 和总延迟。
- 失败用例明细。
- 模型、分块、TopK、阈值和提示配置快照。
- 可供后续候选版本比较的基线运行 ID。
- 真实上传、重新入库、删除和重建的任务与审计记录。
- 首批数据规模和增长预估，作为 T15 分页、容量和性能决策输入。

当前进度（2026-08-03）：

- 已建立严格的评测包 Schema 和无副作用校验命令，覆盖 T13 数量门槛、安全声明、文档引用、关键点同义组、路径越界和可选 SHA-256 校验。
- 已建立显式 `--apply` 的评测包导入工具，可创建受限知识库、上传并等待真实入库、批量创建评测集，并按需启动和等待首个 Baseline；短期令牌只从环境变量读取。
- 已通过真实 Chromium E2E 验证 OIDC、知识库权限授权与撤销、上传、Worker、Embedding、Milvus 混合检索、Rerank、真实模型流式回答、引用证据、重新入库、异步删除和知识库归档，单次运行约 19 秒且测试文档已清理。
- 全量索引重建会影响租户完整索引，已保留为人工批准的独立验收项，不由默认真实链路测试自动执行。
- 已建立公开企业合规法规集：10 份带权威来源和 SHA-256 的公开文档、20 条可回答问题、5 条拒答问题；真实 Baseline/Candidate 均为 25/25 成功，失败用例为 0。
- 已执行租户级全量索引重建，任务 `ca14c984-3147-4238-a252-83df5e5c0788` 成功；4 个旧活跃 `e2e-live-*` 的文档和向量已删除并归档，当前活跃数和关联文档数均为 0。
- 真实运行暴露并修复了“无法确定”“未包含”等拒答表达漏检，保存结果按同一口径重算后 Baseline/Candidate 拒答准确率均为 1.0。
- v1 默认门禁曾因关键点同义表达不完整返回 409；已保持 v1 不可变，另建运行前冻结同义组的 `public-compliance-v2`，结果记录见 `docs/evaluation-baselines/public-compliance-v2-2026-08-04.md`。
- v2 Baseline/Candidate 均为 25/25 成功、失败用例 0，配置快照一致；检索、重排、引用、关键点组覆盖和拒答准确率均通过，默认质量门禁返回 200，审计记录为 `passed: true`。
- 真实运行继续暴露并修复“无法得知”这一拒答表达漏检；只对已持久化结果按同一逻辑重算，没有重跑模型、修改答案或放宽门禁阈值。
- 用户确认可使用生成资料完成工程验收；最终集中验收再次用唯一虚构文档通过 OIDC、授权、Worker、Embedding、Milvus、Rerank、真实流式回答、引用、重入库、删除和归档闭环。
- T13 在“公开真实法规质量基线 + 虚构资料生命周期验证”的批准边界内完成。当前 Chat 中转地址仍没有可核验公开数据条款，内部脱敏资料继续禁止使用公共数据例外；本结论不冒充真实内部业务用户验收。

### T14 — 身份与应用安全加固 `done`

- 验证企业 IdP 的 OIDC Discovery、JWKS、Issuer、Audience 和声明映射。
- 收紧并测试 CORS、Redirect URI 和 Post Logout URI 配置。
- 确认所有密钥只通过运行环境注入，不进入源码、前端资源、日志或测试产物。
- 完成租户越权、IDOR、角色降级和错误 JWT 场景测试。
- 增加上传文件类型、体积、压缩炸弹和恶意内容检查。
- 配置并验证 CSP、`X-Content-Type-Options`、`Referrer-Policy` 和 `Permissions-Policy`；CSP 必须兼容 OIDC 登录、静默续期和 API 连接。

当前进度（2026-08-04）：

- OIDC/JWT 已限制为非对称签名算法，并校验 JWK 签名用途、验证操作和算法一致性；生产环境的 Issuer、JWKS 与 CORS Origin 强制 HTTPS。
- CORS 已从任意方法/请求头收紧到应用实际使用集合；Origin 拒绝通配符、路径、查询和用户信息。
- 前端运行配置已约束同源跳转、HTTPS 远程 API/OIDC、OIDC Scope 和 Client ID；登录返回路径只允许 `/app` 内部路由。
- 前端 Bootstrap CSP 兼容 OIDC iframe 和 API 连接；API 已设置 CSP、`nosniff`、拒绝嵌入、Referrer Policy、Permissions Policy 和禁止缓存。
- 上传新增 MIME、文件签名和 Office 容器安全检查，拒绝压缩炸弹、路径穿越、符号链接、宏、ActiveX 与嵌入对象，并停止接受 `.xlsm`。
- 租户授权保持“先租户、后角色”顺序，新增跨租户管理员拒绝用例；既有创建者和最后 Owner 保护继续保留。
- 已在隔离测试环境执行 8 项非破坏性 DAST，安全头、恶意 Origin、鉴权、路径穿越、TRACE 和 OpenAPI 密钥字段检查全部通过。
- 已建立 PII 分类、脱敏、删除、访问审计与数据保留规则，并记录 Chat、Embedding、Rerank 供应商的数据使用审查边界。
- 应用安全记录见 `docs/security/application-security-t14-2026-08-04.md`，数据治理记录见 `docs/security/data-governance-t14-2026-08-04.md`。

### T15 — 压测与故障恢复测试 `done`

- 并发流式聊天和限流。
- 多用户、多租户权限隔离。
- 批量上传、解析和 Embedding。
- 多 Worker 并发入库、删除、重建和评测。
- Redis、Milvus、PostgreSQL 和模型 API 短暂不可用。
- Worker 中途退出后的重试、补偿和一致性。
- 长会话、超大文档和大量引用。
- 根据 T13 的首批数据规模验证知识库、文档、成员和评测数据集列表的查询上限、分页和搜索需求。

输出必须包含容量建议、并发参数、资源利用率、延迟分位数、错误率、队列积压和失败恢复结果，并与 T13 冻结的业务质量基线对照。

如果无分页列表在首批规模或增长预估下超过响应时间、内存或返回体限制，必须完成服务端分页与搜索；只有明确的数据上限和压测证据支持时才能延期。

完成结果（2026-08-04）：

- 知识库、文档、成员和评测集列表新增有界 `q`、`limit`、`offset`，默认及最大单页为 100；前端客户端同步支持查询参数。
- 健康检查依赖调用增加 5 秒超时；Redis、Milvus、PostgreSQL 分别短暂停止时均快速返回 503，恢复后重新返回 200。
- 模型异常返回清理后的 503/流式错误事件，不泄露供应商异常；Redis 限流默认 fail-closed，Worker 保持 late acknowledgement、失败拒绝、重试和补偿约束。
- 6 份虚构文档在双 Worker 下完成并发上传、Embedding、重入库和删除，最大活跃任务为 6，结束后无队列积压、无残留文档。
- 300 次鉴权控制面读取在并发 10 下错误率 0，P95 为 23.458～55.271 ms；健康接口各 500 次、并发 50，错误率 0。
- 3 路真实流式问答全部成功，首 Token P50/P95 为 11399.5/15579.9 ms，总延迟 P50/P95 为 14690.7/16561.1 ms。
- 容量、资源快照、故障恢复和限制见 `docs/performance/t15-capacity-and-resilience-2026-08-04.md`；按用户要求未修改部署配置。

## 8. 后续产品增强任务

以下任务不应排在 T12～T15 前面，除非业务验收明确要求。

| 编号 | 状态 | 任务 |
| --- | --- | --- |
| E01 | done | 异步任务取消、人工重试和失败重放 |
| E02 | done | 从企业目录搜索用户和群组，避免手工输入 OIDC `sub` 或群组字符串 |
| E03 | done | 飞书连接器管理界面、手动同步、失败详情和配置诊断 |
| E04 | done | 原始文档预览/下载和引用定位到页码、段落或单元格 |
| E05 | done | 评测集修改、归档、复制和批量导入界面 |
| E06 | done | 仅在 T15 的压测结果支持时，继续完善非关键列表的大数据量分页与搜索 |
| E07 | done | Firefox 和 WebKit 浏览器 E2E |
| E08 | done | 前端公共依赖进一步拆包，降低首屏 JS 体积 |
| E09 | done | 清理旧国际化文案和低优先级测试警告 |
| E10 | done | 明确当前商业授权边界，并建立第三方许可证检查策略 |

## 9. 开发约束

1. 不重新选择主技术栈，除非现有组件出现有证据的不可解决问题。
2. 不重新设计多粒度 Chunk 架构。分块、TopK、阈值、Rerank 和提示词变化必须经过同一评测集对比。
3. 不继续循环进行前端视觉重做。现有 Evidence Desk 设计体系保持不变。
4. 不重写已经通过真实浏览器验证的 OIDC/PKCE 流程。
5. 不把 Access Token 写入 `localStorage`。
6. 不在前端伪造任务、文档、评测、指标或聊天历史。
7. 不绕过后端权限检查；前端隐藏按钮不能代替服务端鉴权。
8. 不在 Git 中提交真实 `.env`、密钥、数据库数据、上传文件和运行日志。
9. 数据库结构变化必须创建 Alembic 迁移，并验证空库与已有库升级。
10. 每个任务只解决约定范围，测试通过后再进入下一编号任务。

## 10. 验证命令

代码质量验证命令如下。

### 后端

```powershell
.\.venv\Scripts\python.exe -m ruff check app tests migrations scripts
.\.venv\Scripts\python.exe -m mypy app
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m alembic current
```

### 前端

```powershell
Set-Location frontend
npm ci
npm run typecheck
npm run lint
npm run test
npm run build
npm run test:e2e
Set-Location ..
```

### T13 评测包与真实链路

```powershell
.\.venv\Scripts\python.exe scripts\validate_evaluation_package.py <manifest.json> --repository-root <approved-data-root> --require-t13
.\.venv\Scripts\python.exe scripts\import_evaluation_package.py <manifest.json> --repository-root <approved-data-root>
$env:E2E_LIVE_RAG = "1"
Set-Location frontend
npm run test:e2e -- live-rag.spec.ts
Set-Location ..
```

导入真实资料还必须显式传入 `--apply`，启动首个 Baseline 还需追加 `--start-run`；详细安全步骤见 `docs/t13-business-baseline.md`。全量索引重建不属于上述自动命令。

### 基础验收要求

- 后端检查全部通过。
- 前端 typecheck、lint、test 和 build 全部通过。
- Playwright 不允许存在 `skip`、`only` 或静默失败。
- Alembic 处于 head。
- Git 工作区只包含当前任务范围内的预期修改。

## 11. 当前已知工程状态

- 已提交主线功能完成到评测用例生命周期。
- 最近完整代码验收记录包含后端 169 条 pytest、前端 155 条 Vitest，以及 Chromium、Firefox、WebKit 各 11 条、共 33 条确定性 Playwright；真实 OIDC/RAG 与 T15 负载验收保留既有记录，迁移到新环境后应重新执行，不能只沿用历史数字。
- 确定性 Playwright 默认覆盖 Chromium、Firefox 和 WebKit；依赖真实外部服务的套件继续使用 Chromium 并要求显式开关。
- 前端构建已经完成稳定公共依赖拆包，应用主包由 441.25 kB 降至 175.28 kB（gzip 由 129.58 kB 降至 53.25 kB）。
- 仓库包含工程评测集与两版各 25 条的公开企业合规业务评测集；v2 Baseline/Candidate 门禁已经通过。内部业务资料仍须经过独立批准后才能使用。
- 项目当前处于开发版本 `0.1.0`，尚未发布 `v1.0.0`。

## 12. 完工标准

满足以下全部条件后，项目才能标记为质量加固完成：

- 测试环境的上传、Worker、Embedding、Milvus、真实模型、引用、重新入库、删除和重建链路通过真实 E2E。
- 核心功能、权限和租户隔离通过真实 E2E。
- 真实业务评测集冻结，候选版本质量门禁通过。
- CI 自动执行静态检查、测试、迁移、构建、安全扫描和质量门禁。
- OIDC、跨域、安全头和密钥使用检查完成。
- PII、模型供应商数据使用、数据保留和审计日志保留策略完成。
- 压测、依赖故障恢复和安全验收有可追溯报告。
- 分页和性能问题已经按真实业务规模处理或形成明确限制。

当前主线 T01～T15 和产品增强 E01～E10 均已完成，项目状态可描述为：**完成代码级收尾加固、进入维护阶段的企业 RAG MVP**。真实内部业务推广前仍需由业务与安全负责人独立验收数据授权和供应商条款。

## 13. 任务维护记录

完成任务后，在本节追加记录，同时更新文档顶部的 `current_task`、`next_task` 和 `last_updated`。

记录模板：

```markdown
### YYYY-MM-DD — Txx 任务名称

- 状态：done / in_progress / blocked
- 完成内容：
- 关键修改：
- 验证结果：
- 剩余问题：
- 下一任务：
```

### 2026-07-23 — 项目进度文档建立

- 状态：done
- 完成内容：建立长期维护的项目介绍、已完成能力、当前阶段、未完成任务顺序和完工标准。
- 关键修改：新增 `PROJECT_PROGRESS.md`，使用编号维护主线任务和增强任务。
- 验证结果：文档不包含真实密钥；当前任务明确为 T12。
- 剩余问题：继续完成自动化质量检查、真实业务基线、安全与压测任务。
- 下一任务：继续 T12 CI 与自动质量门禁。

### 2026-08-03 — 删除部署相关任务

- 状态：done
- 完成内容：删除部署拓扑、容器发布、备份灾备、运行手册和版本发布任务。
- 关键修改：保留 CI、真实全链路、应用安全和压测任务，并重新编号为 T12～T15。
- 验证结果：任务编号和前后依赖已同步更新，主线不再包含部署工作。
- 剩余问题：从 T12 的自动化质量门禁继续开发。
- 下一任务：继续 T12 CI 与自动质量门禁。

### 2026-08-03 — T12 自动质量门禁第一阶段

- 状态：in_progress
- 完成内容：建立后端、数据库迁移、前端、确定性 E2E、依赖审计、密钥扫描和 CodeQL 检查。
- 关键修改：新增 GitHub Actions CI；默认 E2E 与外部服务解耦；修复前端锁文件高危依赖；为仅影响未使用 RSC 模式的公告增加带到期日的窄范围例外。
- 验证结果：后端 88 条 pytest、前端 125 条 Vitest、7 条确定性 Playwright、Alembic 临时空库往返、Python/npm 审计和密钥扫描通过。
- 剩余问题：确定项目许可证或商业授权策略并接入许可证门禁；为真实 Baseline/Candidate 质量门禁提供安全的 CI 输入。
- 下一任务：继续 T12，不提前进入 T13。

### 2026-08-03 — T12 完成并进入 T13

- 状态：done
- 完成内容：确认代码质量、确定性测试、迁移兼容、依赖审计、密钥扫描和 SAST 已满足 T12 验收范围。
- 关键修改：真实 Baseline/Candidate 业务评测转入 T13；许可证或商业授权策略转入 E10，均不再阻塞 T12。
- 验证结果：后端 88 条 pytest、前端 125 条 Vitest、7 条确定性 Playwright、Alembic 临时空库往返、Python/npm 审计和密钥扫描通过。
- 剩余问题：准备 T13 所需的脱敏真实文档、标准问题、拒答题和模型额度。
- 下一任务：开始 T13 真实全链路与业务质量基线。

### 2026-08-03 — T13 真实链路与评测包第一阶段

- 状态：in_progress
- 完成内容：建立真实业务评测包安全门禁、dry-run 校验、显式导入与 Baseline 启动工具；扩展真实浏览器链路到权限、重新入库、删除和归档。
- 关键修改：新增评测包 Pydantic Schema、校验与导入脚本、T13 操作文档和对应测试；真实 E2E 具备失败后清理，并禁止自动触发租户级全量索引重建。
- 验证结果：Ruff、mypy、95 条 pytest、125 条 Vitest、前端 typecheck/lint 通过；真实 OIDC/RAG 生命周期 Playwright 1 条通过，耗时约 19 秒。
- 剩余问题：需要提供 10～50 份获批的脱敏真实业务文档、20～50 条标准问题（含 5～10 条拒答题）和首批规模预估；完成 Baseline/Candidate 运行、人工批准的全量索引重建及指标冻结。
- 下一任务：准备并校验真实业务评测包，然后运行和验收首个 Baseline。

### 2026-08-03 — T13 公开合规基线与全量重建

- 状态：in_progress
- 完成内容：从政府和全国人大官方公开来源建立 10 份法规语料、20 条可回答题和 5 条拒答题；完成真实 Baseline、全量索引重建、Candidate、比较和默认质量门禁。
- 关键修改：冻结文档 SHA-256 和公开来源；新增显式维护 Playwright；清理 4 个旧活跃 E2E 知识库；根据真实答案扩展拒答检测并重算两次运行。
- 验证结果：两次运行均 25/25 成功、失败用例 0；检索、重排、引用和重算后拒答准确率均为 1.0；全量重建任务成功；100 条 pytest、125 条 Vitest 和维护 Playwright 通过。
- 剩余问题：默认门禁因关键点组覆盖率从 0.966667 降至 0.933333 返回 409；需新建不可变的 v2 同义组评测集并通过门禁。当前 Chat 中转条款无法公开核验，内部数据不得使用公共数据例外。
- 下一任务：建立公开合规 v2 同义组并重新运行 Baseline/Candidate，随后再接入经正式批准的内部脱敏业务集。

### 2026-08-04 — T13 公开合规 v2 质量门禁通过

- 状态：in_progress
- 完成内容：保持 v1 不可变，建立运行前冻结同义答案组的公共合规 v2；完成真实 Baseline、Candidate、指标重算、比较和默认质量门禁。
- 关键修改：新增 v2 清单与固定哈希测试；显式维护 E2E 支持选择 v1/v2 并要求 v2 门禁返回 200；补齐“无法得知”拒答表达检测及回归测试；冻结 v2 评测报告。
- 验证结果：两次运行均 25/25 成功且失败用例为 0，配置快照一致；关键点组覆盖率和拒答准确率均为 1.0；质量门禁审计为 `passed: true`、`failed_metrics: []`；后端 102 条 pytest、前端 125 条 Vitest、7 条默认 Playwright、1 条维护 Playwright、Ruff、mypy、类型检查、Lint、构建、依赖完整性和密钥扫描均通过。
- 剩余问题：需要经业务与安全负责人批准的内部脱敏业务包、当前模型供应商数据处理结论及首批规模预估；公共数据例外不得用于内部资料。
- 下一任务：取得外部审批与内部数据后完成内部业务基线；在此之前不进入 T14，也不改动部署相关内容。

### 2026-08-04 — 转入 T14 应用安全加固

- 状态：in_progress
- 完成内容：用户确认后续可使用生成的合成业务资料测试，并要求先继续下一阶段、全部开发完成后再统一测试；因此将合成业务验收延后到最终集中验收，不再阻塞应用代码加固。
- 关键修改：收紧 OIDC/JWK、CORS、前端运行配置和 OIDC 返回路径；增加应用层 CSP 与 API 安全响应头；建立文件签名、Office 容器和压缩炸弹上传门禁。
- 验证结果：待本阶段全部实现完成后集中执行并补记。
- 剩余问题：执行完整后端、前端和浏览器回归，根据结果修复问题；静态文件服务器响应头属于部署配置，本轮按用户要求不修改。
- 下一任务：完成 T14 集中验收，通过后进入 T15 容量与性能验证。

### 2026-08-04 — T14 应用安全与数据治理完成

- 状态：done
- 完成内容：收紧 OIDC/JWK、CORS、前端运行配置、跳转路径和安全响应头；建立上传容器安全、跨租户授权和数据治理规则。
- 关键修改：新增 Office/压缩包安全检查、CSP、请求 ID 约束、生产 URL 校验、供应商审查记录和非破坏性 DAST。
- 验证结果：DAST 8/8 通过；租户、JWT、上传和安全头自动测试进入后端全量回归；358 个文本文件密钥扫描通过。
- 剩余问题：当前 Chat 中转服务没有可核验公开数据条款，内部资料继续禁止发送；自动到期物理清除仍按数据治理文档中的人工流程执行。
- 下一任务：T15 压测与故障恢复测试。

### 2026-08-04 — T15 容量与故障恢复完成

- 状态：done
- 完成内容：完成有界列表查询、并发读取、批量异步文档生命周期、真实流式问答、依赖故障注入、Worker 恢复与资源快照。
- 关键修改：列表增加分页和搜索；健康依赖检查增加超时；模型错误清理；新增负载探针、恢复测试和容量报告。
- 验证结果：后端 128 条 pytest、前端 137 条 Vitest、7 条确定性 Playwright、6 条真实 OIDC/RAG Playwright、2 条 T15 负载 Playwright 全部通过；Ruff、mypy、类型检查、Lint、构建、依赖完整性、迁移 head、DAST 和密钥扫描通过。300 次控制面请求、1000 次健康探针和 3 路真实流式回答错误率均为 0；临时数据与队列无残留。
- 剩余问题：1000 条以上真实业务列表、长时间稳态流量和资源峰值需在实际首批规模可用后重新校准；不属于本轮部署范围。
- 下一任务：主线 T01～T15 完成；按优先级进入 E01 产品增强。

### 2026-08-08 — E01 异步任务取消、重试与失败重放

- 状态：done
- 完成内容：文档入库、重入库、删除、目录扫描、全量索引重建和评测运行支持安全取消与关联重试；任务中心和评测详情提供真实操作入口。
- 关键修改：新增 `cancelled` 终态、取消人/时间、`retry_of_job_id` 与 `retry_of_run_id`；增加行锁、Worker 启动检查、活跃重试唯一约束、Celery 非强制 revoke 和权限审计；评测配置变化时拒绝伪重放。
- 验证结果：Alembic 0010 已完成现有数据库升级、回退和再次升级；Ruff、mypy、前端类型检查、Lint、构建和依赖完整性检查通过；后端 137 条 pytest、前端 141 条 Vitest、8 条确定性 Chromium Playwright 全部通过；362 个文本文件的密钥扫描通过，数据库无排队或运行中的入库/评测任务残留。
- 剩余问题：运行中的任务不提供强制终止，这是保护 PostgreSQL/Milvus 和逐用例结果一致性的明确边界；如未来需要协作式中止，应另行设计安全检查点。
- 下一任务：E02 企业目录用户与群组搜索。

### 2026-08-08 — E02 企业目录用户与群组搜索

- 状态：done
- 完成内容：知识库 owner 可从 Keycloak 企业目录搜索用户和群组，按 20 条分页选择真实主体后新增或更新 ACL；前端不再允许手工填写 OIDC `sub` 或群组字符串。
- 关键修改：新增受 owner 权限保护的 `directory-principals` API、Keycloak client-credentials 只读目录服务、服务令牌按有效期复用、单租户目录绑定、禁用用户过滤、嵌套群组与 `name`/`path` claim 映射、300 毫秒搜索防抖和结果选择校验；用户主体保持严格 OIDC subject 校验，群组支持合法空格并拒绝首尾空白和控制字符。
- 验证结果：Ruff、mypy、前端类型检查、Lint、构建和依赖完整性检查通过；后端 143 条 pytest、前端 143 条 Vitest、8 条确定性 Chromium Playwright 全部通过；365 个文本文件的密钥扫描通过，运行中 OpenAPI 已发布搜索接口且开发环境健康。
- 配置边界：目录功能默认关闭；真实环境需从运行时密钥来源提供与单一应用租户绑定的 Keycloak 只读服务账户，并授予最小用户/群组查询权限。本任务未修改部署配置，自动化验收使用生成的脱敏目录数据和模拟 Keycloak 响应。
- 下一任务：E03 飞书连接器管理界面、手动同步、失败详情和配置诊断。

### 2026-08-08 — E03 飞书连接器管理与手动同步

- 状态：done
- 完成内容：新增仅管理员可见的飞书连接器管理页，可检查安全配置摘要、诊断应用凭据与 Wiki 空间连通性、手动创建持久化同步任务，并查看任务进度、同步统计和结构化失败详情。
- 关键修改：新增连接器状态、诊断和同步 API；App Secret 使用密文类型且 API 仅返回配置布尔值；飞书上游错误保留错误码、`x-tt-logid`、失败操作和可重试标志；Wiki 分页兼容权限过滤空页并防止游标循环；手动同步加入租户级活跃任务唯一约束、审计、取消/重试和 Worker 状态落库；前端新增中英文管理界面与管理员导航。
- 验证结果：Alembic 0011 已完成现有数据库升级、回退和再次升级；Ruff、mypy、pip check、前端 typecheck、Lint、构建、`npm audit` 和依赖重装通过；后端 152 条 pytest、前端 146 条 Vitest、9 条确定性 Chromium Playwright 全部通过。
- 配置边界：真实飞书连通性仍需业务环境提供获批的只读应用凭据、Wiki 空间权限和目标知识库运行身份；本轮自动化使用生成数据和模拟飞书响应，未修改任何部署配置。
- 详细说明：`docs/e03-feishu-connector.md`。
- 下一任务：E04 原始文档预览/下载和引用定位到页码、段落或单元格。

### 2026-08-09 — E04 原文预览、下载与引用定位

- 状态：done
- 完成内容：文档 reader 可预览当前已发布索引、经鉴权下载原文件；当前回答和历史会话引用显示页码、幻灯片、段落、单元格或章节位置，并可直接打开命中位置及相邻原文。
- 关键修改：新增受权限保护的 preview/download API、稳定位置白名单和预览审计；解析器为 TXT、Markdown、DOCX、CSV、XLS/XLSX 补充精确位置元数据；检索引用携带安全位置；前端新增响应式原文对话框和带 Token 的二进制下载。`DocumentRead` 仅暴露 `source_available`，不再返回服务器 `source_uri`。
- 验证结果：Ruff、mypy、pip check、前端 typecheck、Lint、生产构建和 `npm audit` 通过；后端 159 条 pytest、前端 148 条 Vitest、10 条确定性 Chromium Playwright 全部通过；浏览器覆盖引用定位跳转、单元格预览和鉴权下载。
- 配置边界：复用现有 `source_metadata` 和源文件，不需要数据库迁移；自动化使用生成的脱敏内容，未修改任何部署配置。
- 详细说明：`docs/e04-document-source-navigation.md`。
- 下一任务：E05 评测集修改、归档、复制和批量导入界面。

### 2026-08-16 — E05 评测集生命周期与批量导入

- 状态：done
- 完成内容：评测集支持修改名称和描述、复制全部用例、可追溯归档和活动/归档状态筛选；编辑者可通过 JSON 文件或粘贴内容预校验并原子导入最多 500 条用例。
- 关键修改：新增 update/copy/archive API 和审计；复制不复制运行与结果；归档前阻止活动运行，归档后历史用例、运行、报告、对比和门禁保持只读可访问；数据集行锁串行化归档、用例写入和新运行；前端增加中英文管理面板、显式归档确认、JSON 题型计数及错误展示。
- 验证结果：Ruff、mypy、pip check、前端 typecheck、Lint、生产构建和 `npm audit` 通过；后端 166 条 pytest、前端 154 条 Vitest、11 条确定性 Chromium Playwright 全部通过。
- 配置边界：复用现有 `evaluation_datasets.status` 与批量用例接口，不需要数据库迁移；自动化使用生成的脱敏用例，未修改任何部署配置。
- 详细说明：`docs/e05-evaluation-dataset-management.md`。
- 下一任务：E06 根据 T15 压测证据复核非关键列表；仅对证据显示需要改进的列表继续分页与搜索。

### 2026-08-16 — E06 非关键列表容量复核

- 状态：done
- 完成内容：逐项复核知识库、文档、成员、评测集、任务、会话、消息、审计、评测用例和评测运行的服务端边界与前端消费方式。
- 关键结论：T15 当前规模为 3 个知识库、20 份文档、10 条成员关系、13 个会话和 2 个评测集；控制面 P95 为 23.458～55.271 ms、错误率 0。所有集合接口已有显式上限，关键历史列表已有总数分页；未达到报告规定的单租户 1000 条复测阈值，因此不新增无证据收益的分页、总数查询或搜索索引。
- 关键修改：新增 OpenAPI 自动契约测试，固定十类集合接口的默认/最大页大小、非负 offset 和搜索参数；新增容量复核记录和重新实施触发条件，业务功能代码保持不变。
- 验证结果：Ruff、mypy、pip check 和后端全量 167 条 pytest 通过，其中控制面历史与 T15 负载探针相关测试 13 条；OpenAPI 盘点确认列表上限为 100，会话消息上限为 200 且前端每次只加载 50 条。
- 配置边界：没有数据库迁移、前端行为变化或部署配置变更。真实首批内部业务规模达到数量级差异后必须重新压测，不能把本次延期理解为永久不做分页。
- 详细说明：`docs/e06-list-capacity-review.md`。
- 下一任务：E07 Firefox 和 WebKit 浏览器 E2E。

### 2026-08-16 — E07～E10 收尾加固完成

- 状态：done
- 完成内容：确定性 Playwright 默认扩展到 Chromium、Firefox 和 WebKit；拆分前端公共依赖；清理旧导航占位、国际化文案和 React 测试警告；明确当前书面授权边界并建立第三方许可证允许清单门禁。
- 关键修改：在不放宽 CSP 的情况下启用 Zod jitless 验证；修复跨浏览器导航竞争和严格定位歧义；建立稳定 Vite 分包；新增项目依赖闭包许可证检查、CI 步骤和合规说明。没有新增开源 `LICENSE`，也没有修改部署配置或数据库结构。
- 验证结果：后端 169 条 pytest、前端 155 条 Vitest、Chromium/Firefox/WebKit 共 33 条确定性 Playwright 全部通过；生产预览 Chromium 11 条通过；Ruff、mypy、typecheck、Lint、构建、pip check、npm audit、许可证门禁和密钥扫描通过。
- 剩余问题：真实内部业务推广仍需使用获批数据和真实凭据完成业务、安全及供应商条款验收；这属于外部准入，不影响当前代码基线进入维护阶段。
- 详细说明：`docs/e07-e10-final-hardening.md`。
- 下一任务：无固定开发任务；后续按真实业务反馈进入维护迭代。
