# E07～E10 收尾加固与验收记录

日期：2026-08-16

## 结论

E07～E10 已完成，产品增强待办进入维护状态。本轮只调整测试、前端代码质量、构建拆包和许可证合规门禁，没有修改部署配置或数据库结构。

## E07 跨浏览器端到端测试

- 确定性 Playwright 套件默认覆盖 Chromium、Firefox 和 WebKit。
- 真实 OIDC、真实 RAG、维护和负载套件继续限定在 Chromium，避免默认质量门禁依赖外部凭据或服务。
- 修复 Firefox 下严格 CSP 与 Zod 运行时代码生成冲突，保持 `script-src 'self'`，没有放宽为 `unsafe-eval`。
- 修复跨浏览器退出登录后的异步导航竞争，以及任务控制用例中的严格定位歧义。
- 最终结果：三个浏览器各 11 条，共 33 条确定性 E2E 全部通过。
- WebKit 在隔离的 Ubuntu 24.04 测试容器中运行；临时容器和镜像已在测试后删除。

## E08 前端公共依赖拆包

Vite 构建按 React、TanStack Query、i18n、OIDC、表单和 Zod 建立稳定的公共依赖分包。

| 指标 | 调整前 | 调整后 |
| --- | ---: | ---: |
| 应用主包 | 441.25 kB | 175.28 kB |
| 应用主包 gzip | 129.58 kB | 53.25 kB |
| 入口包 | 199.78 kB | 6.26 kB |
| 入口包 gzip | 63.48 kB | 2.83 kB |

生产预览环境中的 11 条 Chromium 确定性 E2E 同样通过。

## E09 警告与旧文案清理

- 消除评测控制台测试中的 React `act` 警告，并等待异步报告查询稳定后再交互。
- 删除不可达的路由占位组件、导航“即将推出”分支及对应中英文旧文案和样式。
- 前端 155 条 Vitest 全部通过，控制台没有遗留测试警告。

## E10 授权边界与第三方许可证门禁

- 当前不新增开源 `LICENSE`；仓库默认保留全部权利，复制、修改或再分发需要维护者书面授权。
- 新增 Python 与 npm 实际安装依赖的 SPDX 许可证允许清单检查，并接入 CI。
- 缺失或不在允许清单内的许可证会阻断检查；需要附带声明的依赖会单独列出。
- 本次检查覆盖 555 个 Python/npm 依赖，结果通过；许可证检查自身的 2 条测试通过。

该策略解决当前授权边界和第三方依赖门禁，不等同于已经选择未来的开源许可证。若后续计划对外发布，仍需由权利人确定项目许可证，并随制品提供适用的第三方声明。

## 最终自动化结果

- 后端：Ruff、mypy、pip 依赖完整性和许可证检查通过；169 条 pytest 通过。
- 前端：typecheck、ESLint、生产构建和 npm audit 通过；155 条 Vitest 通过。
- 浏览器：Chromium、Firefox、WebKit 共 33 条确定性 E2E 通过；生产预览 Chromium 11 条通过。
- 安全：仓库密钥扫描通过。

自动化结果证明当前代码基线可以进入维护阶段。真实内部业务上线仍需使用获批数据、真实凭据和实际供应商条款完成独立业务与安全验收。
